{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Job Fetcher",
      "type": "python",
      "request": "launch",
      "module": "ingestion.job_fetcher",
      "cwd": "${workspaceFolder}"
    }
  ]
}